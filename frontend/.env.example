# Copy to .env.local for local overrides, or set as a build-time
# environment variable on your hosting platform (Vercel/Netlify project
# settings). Must point at the deployed backend's /api path.
VITE_API_BASE=http://127.0.0.1:8000/api
